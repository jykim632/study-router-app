export type DashboardCardProps = {
  title?: string;
  description?: string;
  content?: React.ReactNode;
  footer?: React.ReactNode;
  classNames?: string;
};

export type DashboardCountCardProps = {
  count: number;
};

export type DashboardListCardProps = {};
